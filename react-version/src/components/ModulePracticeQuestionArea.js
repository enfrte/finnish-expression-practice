import React, { useContext } from 'react';

// Details about the module and link to execute the practice
const ModuleAnswerArea = ( {module} ) => {
  return (
    <div className="">
      asdf - { module.title }
    </div>
  );
}

export default ModuleAnswerArea;